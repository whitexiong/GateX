package projects

import (
	"github.com/gin-gonic/gin"
)

func SetupProjectAPIEndpoints(r *gin.Engine) {
	//group := r.Group("/projects/:projectId/api-endpoints")

	//group.POST("/list", projects.ListProjectAPIEndpoints)
	//group.POST("/add", projects.CreateProjectAPIEndpoint)
	//group.GET("/details/:endpointId", projects.GetProjectAPIEndpointDetails)
	//group.PUT("/update/:endpointId", projects.UpdateProjectAPIEndpoint)
	//group.GET("/delete/:endpointId", projects.DeleteProjectAPIEndpoint)
	//group.GET("/init", projects.DeleteProjectAPIEndpoint)
}
